# Source: gatekeeper/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gatekeeper
  labels:
    helm.sh/chart: gatekeeper-0.1.39
    app.kubernetes.io/name: gatekeeper
    app.kubernetes.io/instance: gatekeeper
    app.kubernetes.io/version: "2.8.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: gatekeeper
      app.kubernetes.io/instance: gatekeeper
  template:
    metadata:
      annotations:
        checksum/config: "d0d291a8592eadfdd487ec172f3f66453690b87c6506f151c95e51b724c0cbbb"
      labels:
        app.kubernetes.io/name: gatekeeper
        app.kubernetes.io/instance: gatekeeper
    spec:
      automountServiceAccountToken: false
      serviceAccountName: gatekeeper
      containers:
        - name: gatekeeper
          securityContext:
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          image: "harbor.taco-cat.xyz/tks/gatekeeper:2.8.0"
          imagePullPolicy: IfNotPresent
          args:
            - --config
            - /etc/gatekeeper/config.yaml
          ports:
            - name: proxy
              containerPort: 3000
              protocol: TCP
            - name: admin
              containerPort: 4000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /oauth/health
              port: admin
          resources:
            limits: {}
            requests: {}
          volumeMounts:
            - mountPath: /etc/gatekeeper
              name: config
      volumes:
        - secret:
            secretName: gatekeeper
          name: config

